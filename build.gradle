buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0' // Use the latest version
        classpath 'com.google.firebase:firebase-crashlytics-gradle:2.9.9' // Latest Firebase Crashlytics plugin
        classpath 'com.google.gms:google-services:4.4.0' // Update if needed
    }
}
